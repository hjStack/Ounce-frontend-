# Include this inside the frontend server block, outside location blocks.
# If the same server block proxies /api, keep API locations on proxy_intercept_errors off.
proxy_intercept_errors on;
error_page 500 502 503 504 /maintenance.html;

location = /maintenance.html {
    root /var/www/ounce-error;
    internal;
}
